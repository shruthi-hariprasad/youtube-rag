# Eval-only dependencies — install locally to run the RAGAS harness.
# NOT installed on Render (production doesn't need these).
#
# Usage:
#   pip install -r requirements-eval.txt
ragas==0.4.3
deepeval==2.9.3
datasets==4.8.5
langchain-huggingface==1.2.2
