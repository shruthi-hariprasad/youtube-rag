[pytest]
markers =
    eval: slow evaluation tests (require DB + GROQ_API_KEY)
    smoke: quick sanity check on 8 samples (~2 min)
    full: complete 75-question suite (~20 min)
    retrieval: retrieval comparison table (BM25 / Dense / Hybrid)
